function setup() {
  // put setup code here
  createCanvas(500, 500);
}

function draw() {
  background(0);
  fill(255, 0, 0);
  ellipse(0, width / 2, 100, 90);
  rect(width / 2, height / 2, width / 3, height / 5);
}